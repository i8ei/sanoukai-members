---
import BaseLayout from "../../layouts/BaseLayout.astro";
import { getYouthMembers } from "../../lib/csvMembers";

const youthMembers = await getYouthMembers();
---

<BaseLayout
  title="青年会員一覧 | 佐賀県農業法人協会"
  description="佐賀県農業法人協会の青年会員一覧ページ"
>
  <section class="mx-auto max-w-5xl space-y-6">
    <article
      class="rounded-xl border border-line bg-card p-6 shadow-card md:p-8"
    >
      <h1 class="text-2xl font-black tracking-tight text-ink md:text-3xl">
        青年会員一覧
      </h1>
      <p class="mt-3 text-base leading-relaxed text-ink-soft">
        佐賀県農業法人協会の次代を担う青年会員のご紹介です。
      </p>
    </article>

    <article class="grid grid-cols-1 gap-4 md:grid-cols-3">
      {
        youthMembers.map((m) => (
          <div class="rounded-xl border border-line bg-card p-5 shadow-card transition-all duration-300 hover:shadow-card-hover hover:-translate-y-0.5">
            <p class="text-lg font-bold text-ink">{m.name}</p>
            <p class="mt-1 text-base text-ink-soft">{m.representative}</p>
            <p class="mt-2 text-sm text-ink-soft">{m.address}</p>
            {m.businessSummary && (
              <p class="mt-2 text-sm text-ink-soft bg-bg p-2 rounded-md">
                {m.businessSummary}
              </p>
            )}
          </div>
        ))
      }
    </article>
  </section>
</BaseLayout>
