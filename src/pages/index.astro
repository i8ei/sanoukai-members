---
import BaseLayout from '../layouts/BaseLayout.astro';
import { getGeneratedAt, getMembers } from '../lib/members';

const members = await getMembers();
const generatedAt = await getGeneratedAt();
const BASE = import.meta.env.BASE_URL.endsWith('/') ? import.meta.env.BASE_URL : `${import.meta.env.BASE_URL}/`;
const generatedAtText = generatedAt ? new Date(generatedAt).toLocaleString('ja-JP') : '情報準備中';
---

<BaseLayout title="会員紹介トップ | 佐賀県農業法人協会" description="佐賀県農業法人協会 会員法人の紹介サイト">

  <!-- ═══ Hero Section ═══ -->
  <section class="hero-section -mx-4 -mt-6 mb-8 rounded-none px-4 py-16 sm:-mx-6 sm:px-6 md:-mt-10 md:mb-12 md:py-24">
    <div class="relative z-10 mx-auto max-w-3xl text-center">
            <h1 class="mt-4 animate-fade-in-up text-3xl font-black tracking-tight text-white sm:text-4xl md:text-5xl text-balance">佐賀県農業法人協会</h1>
      <p class="mt-5 animate-fade-in-up text-base leading-7 text-white/85 stagger-1 sm:text-lg">
        佐賀県内で農業法人として連携を広げたい方向けの案内サイトです。<br />
        まずは会員募集ページをご確認ください。
      </p>
      <div class="mt-8 flex flex-wrap items-center justify-center gap-3 animate-fade-in-up stagger-2">
        <a href={`${BASE}join/`} class="inline-flex items-center gap-2 rounded-xl bg-white px-6 py-3 text-sm font-bold text-brand no-underline shadow-hero transition-all duration-200 hover:bg-sun-pale hover:shadow-lg hover:no-underline hover:-translate-y-0.5">
          <span>✅</span> 入会申込
        </a>
        <a href={`${BASE}members/`} class="inline-flex items-center gap-2 rounded-xl border border-white/40 bg-white/10 px-6 py-3 text-sm font-bold text-white no-underline transition-all duration-200 hover:bg-white/20 hover:no-underline">
          <span>🌾</span> 会員一覧を見る
        </a>
      </div>
    </div>
  </section>

  <div class="space-y-6 md:space-y-8">

    <!-- ═══ Stats Bar ═══ -->

    <!-- ═══ 協会について（統合） ═══ -->
    <article class="rounded-xl border border-line bg-section-green p-5 shadow-card md:p-6 animate-fade-in-up stagger-4">
      <h2 class="flex items-center gap-2 text-sm font-bold text-ink">
        <span class="text-base">🏛️</span> 佐賀県農業法人協会について
      </h2>
      <p class="mt-3 text-sm leading-7 text-ink-soft">
        佐賀県農業法人協会は、会員同士のつながりを強め、経営力向上と地域農業の持続的発展を目指す団体です。
        情報共有・連携・実践支援を通じて、各法人の成長と地域価値の向上に取り組んでいます。
      </p>

      <div class="mt-4 grid grid-cols-1 gap-3 md:grid-cols-3">
        <div class="rounded-lg border border-line bg-card p-3">
          <p class="text-sm font-semibold text-ink">🌱 つながる</p>
          <p class="mt-1 text-xs leading-6 text-ink-soft">会員同士の情報共有・相談で、課題を一人で抱え込まない体制をつくります。</p>
        </div>
        <div class="rounded-lg border border-line bg-card p-3">
          <p class="text-sm font-semibold text-ink">📈 伸ばす</p>
          <p class="mt-1 text-xs leading-6 text-ink-soft">経営・販路・人材の観点から、各法人の成長につながる実践的な取り組みを進めます。</p>
        </div>
        <div class="rounded-lg border border-line bg-card p-3">
          <p class="text-sm font-semibold text-ink">🌾 守り育てる</p>
          <p class="mt-1 text-xs leading-6 text-ink-soft">地域農業の持続性を高め、次世代につながる土台づくりを大切にしています。</p>
        </div>
      </div>

      <h3 class="mt-4 text-sm font-semibold text-ink">主な活動内容</h3>
      <ul class="mt-2 space-y-2 text-sm leading-7 text-ink-soft">
        <li>・会員法人の情報共有と相互連携の促進</li>
        <li>・生産・販路・経営に関する実践的な支援</li>
        <li>・次世代へつながる地域農業基盤づくり</li>
      </ul>

      <h3 class="mt-4 text-sm font-semibold text-ink">入会するとできること</h3>
      <ul class="mt-2 space-y-2 text-sm leading-7 text-ink-soft">
        <li>・同じ立場の法人同士で課題相談・情報交換ができる</li>
        <li>・販路や生産の知見を共有し、判断の精度を上げられる</li>
        <li>・地域と連携した取り組みを広げやすくなる</li>
      </ul>
    </article>

  </div>
</BaseLayout>
