---
import BaseLayout from '../../layouts/BaseLayout.astro';
import Badge from '../../components/Badge.astro';
import { briefProducts, fallback, getMemberById, getMembers, isPresent } from '../../lib/members';

export async function getStaticPaths() {
  const members = await getMembers();
  return members.map((member) => ({
    params: { id: member.id }
  }));
}

const id = Astro.params.id ?? '';
const member = await getMemberById(id);
const BASE = import.meta.env.BASE_URL.endsWith('/') ? import.meta.env.BASE_URL : `${import.meta.env.BASE_URL}/`;

if (!member) {
  throw new Error(`Member not found: ${id}`);
}

const toTextList = (items: unknown): string[] => {
  if (!Array.isArray(items)) return [];
  return items
    .map((value) => String(value ?? '').trim())
    .filter((value) => value.length > 0);
};

const channels = toTextList(member.sales_channels);
const productNames = (member.products ?? [])
  .map((product) => product.name || product.category)
  .filter((value): value is string => Boolean(value && value.trim()));
const summary = member.business?.summary;
const certifications = toTextList(member.certifications);
const awards = (member.awards ?? []).filter((award) => isPresent(award?.title));
const sns = toTextList(member.web_presence?.sns);
const areaCode = member.area?.code ?? 'default';
const dotClass = `area-dot area-dot-${areaCode}`;
---

<BaseLayout title={`${fallback(member.name)} | 会員詳細`}>
  <section class="space-y-5 md:space-y-6">

    <!-- ═══ Member Header ═══ -->
    <article class="relative overflow-hidden rounded-2xl border border-line bg-gradient-to-br from-brand-dark via-brand to-leaf p-6 shadow-card md:p-8">
      <div class="absolute inset-0 opacity-5" style="background-image: url(&quot;data:image/svg+xml,%3Csvg width='60' height='60' viewBox='0 0 60 60' xmlns='http://www.w3.org/2000/svg'%3E%3Ccircle cx='30' cy='30' r='2' fill='white' /%3E%3C/svg%3E&quot;);"></div>
      <div class="relative z-10">
        <p class="text-xs font-semibold tracking-wider text-white/60 uppercase">会員詳細</p>
        <h1 class="mt-2 text-2xl font-black text-white md:text-3xl animate-fade-in-up">{fallback(member.name)}</h1>
        <div class="mt-5 grid gap-3 sm:grid-cols-2 lg:grid-cols-4">
          <div class="rounded-xl bg-white/10 p-3 backdrop-blur-sm">
            <p class="text-xs font-medium text-white/60">エリア</p>
            <p class="mt-1 flex items-center gap-1.5 text-sm font-bold text-white">
              <span class={dotClass}></span>
              {fallback(member.area?.label)}
            </p>
          </div>
          <div class="rounded-xl bg-white/10 p-3 backdrop-blur-sm">
            <p class="text-xs font-medium text-white/60">主な生産品</p>
            <p class="mt-1 text-sm font-bold text-white">{briefProducts(member, 2)}</p>
          </div>
          <div class="rounded-xl bg-white/10 p-3 backdrop-blur-sm">
            <p class="text-xs font-medium text-white/60">販路</p>
            <div class="mt-1 flex flex-wrap gap-1.5">
              {channels.length > 0 ? channels.slice(0, 4).map((channel) => (
                <span class="inline-flex items-center rounded-full border border-white/20 bg-white/10 px-2.5 py-0.5 text-xs text-white">{channel}</span>
              )) : <span class="text-sm text-white/80">情報準備中</span>}
            </div>
          </div>
        </div>
      </div>
    </article>

    <!-- ═══ Basic Info ═══ -->
    <article class="accent-border-brand rounded-xl border border-line bg-card p-5 shadow-card md:p-6 animate-fade-in-up stagger-1">
      <h2 class="flex items-center gap-2 text-sm font-bold text-ink">
        <span class="flex h-7 w-7 items-center justify-center rounded-lg bg-brand/10 text-sm">📋</span>
        基本情報
      </h2>
      <dl class="mt-4 grid gap-3 text-sm sm:grid-cols-2">
        <div class="rounded-xl bg-bg-soft p-3">
          <dt class="text-xs font-medium text-ink-soft">法人種別</dt>
          <dd class="mt-1 font-medium text-ink">{fallback(member.entity_type)}</dd>
        </div>
        <div class="rounded-xl bg-bg-soft p-3">
          <dt class="text-xs font-medium text-ink-soft">所在地</dt>
          <dd class="mt-1 font-medium text-ink">{fallback(member.location?.address || member.location?.city)}</dd>
        </div>
        <div class="rounded-xl bg-bg-soft p-3">
          <dt class="text-xs font-medium text-ink-soft">代表者</dt>
          <dd class="mt-1 font-medium text-ink">{fallback(member.contact?.representative ?? undefined)}</dd>
        </div>
        <div class="rounded-xl bg-bg-soft p-3">
          <dt class="text-xs font-medium text-ink-soft">電話</dt>
          <dd class="mt-1 font-medium text-ink">{fallback(member.contact?.phone ?? undefined)}</dd>
        </div>
        <div class="rounded-xl bg-bg-soft p-3">
          <dt class="text-xs font-medium text-ink-soft">設立</dt>
          <dd class="mt-1 font-medium text-ink">{fallback(member.business?.established ?? undefined)}</dd>
        </div>
        <div class="rounded-xl bg-bg-soft p-3">
          <dt class="text-xs font-medium text-ink-soft">従業員</dt>
          <dd class="mt-1 font-medium text-ink">{fallback(member.business?.employees ?? undefined)}</dd>
        </div>
      </dl>
    </article>

    <!-- ═══ Products ═══ -->
    <article class="accent-border-leaf rounded-xl border border-line bg-card p-5 shadow-card md:p-6 animate-fade-in-up stagger-2">
      <h2 class="flex items-center gap-2 text-sm font-bold text-ink">
        <span class="flex h-7 w-7 items-center justify-center rounded-lg bg-leaf-pale text-sm">🌱</span>
        生産
      </h2>
      {productNames.length > 0 ? (
        <ul class="mt-4 space-y-2 text-sm text-ink">
          {(member.products ?? []).map((product) => (
            <li class="rounded-xl border border-line bg-card-soft p-3.5 transition-colors hover:bg-earth-pale">
              <p class="font-bold">{fallback(product.name || product.category)}</p>
              {isPresent(product.description) ? <p class="mt-1 text-ink-soft leading-relaxed">{product.description}</p> : null}
            </li>
          ))}
        </ul>
      ) : (
        <p class="mt-4 text-sm text-ink">情報準備中</p>
      )}
    </article>

    <!-- ═══ Sales ═══ -->
    <article class="accent-border-sun rounded-xl border border-line bg-card p-5 shadow-card md:p-6 animate-fade-in-up stagger-3">
      <h2 class="flex items-center gap-2 text-sm font-bold text-ink">
        <span class="flex h-7 w-7 items-center justify-center rounded-lg bg-sun-pale text-sm">🛒</span>
        販売
      </h2>
      <div class="mt-4 flex flex-wrap gap-2">
        {channels.length > 0 ? channels.map((channel) => <Badge label={channel} />) : <p class="text-sm text-ink">情報準備中</p>}
      </div>
      <div class="mt-5 grid gap-3 text-sm sm:grid-cols-2">
        <div class="rounded-xl bg-bg-soft p-3">
          <p class="text-xs font-medium text-ink-soft">Webサイト</p>
          {isPresent(member.web_presence?.website) ? (
            <a href={member.web_presence?.website ?? '#'} target="_blank" rel="noreferrer" class="mt-1 inline-block break-all text-sm">
              {member.web_presence?.website}
            </a>
          ) : (
            <p class="mt-1 text-ink">情報準備中</p>
          )}
        </div>
        <div class="rounded-xl bg-bg-soft p-3">
          <p class="text-xs font-medium text-ink-soft">SNS</p>
          <p class="mt-1 text-ink">{sns.length > 0 ? sns.join(' / ') : '情報準備中'}</p>
        </div>
      </div>
    </article>

    <!-- ═══ Notes ═══ -->
    <article class="accent-border-water rounded-xl border border-line bg-card p-5 shadow-card md:p-6 animate-fade-in-up stagger-4">
      <h2 class="flex items-center gap-2 text-sm font-bold text-ink">
        <span class="flex h-7 w-7 items-center justify-center rounded-lg bg-water/10 text-sm">📝</span>
        補足メモ
      </h2>
      <div class="mt-4 space-y-3 text-sm text-ink">
        <div class="rounded-xl bg-bg-soft p-3">
          <p class="text-xs font-medium text-ink-soft">事業概要</p>
          <p class="mt-1">{isPresent(summary) ? summary : '情報準備中'}</p>
        </div>
        <div class="rounded-xl bg-bg-soft p-3">
          <p class="text-xs font-medium text-ink-soft">認証</p>
          <p class="mt-1">{certifications.length > 0 ? certifications.join(' / ') : '情報準備中'}</p>
        </div>
        <div class="rounded-xl bg-bg-soft p-3">
          <p class="text-xs font-medium text-ink-soft">受賞歴</p>
          <p class="mt-1">
            {awards.length > 0
              ? awards.map((award) => [award.year, award.title].filter((part) => isPresent(part)).join(' ')).join(' / ')
              : '情報準備中'}
          </p>
        </div>
        <div class="rounded-xl bg-bg-soft p-3">
          <p class="text-xs font-medium text-ink-soft">店頭展開メモ</p>
          <p class="mt-1">{fallback(member.shelf_potential ?? undefined)}</p>
        </div>
      </div>
    </article>

    <!-- ═══ Back Link ═══ -->
    <div>
      <a href={`${BASE}members/`} class="inline-flex items-center gap-2 rounded-xl bg-brand px-5 py-2.5 text-sm font-bold text-white no-underline shadow-sm transition-all duration-200 hover:bg-brand-dark hover:shadow-md hover:no-underline hover:-translate-y-0.5">
        ← 会員一覧に戻る
      </a>
    </div>
  </section>
</BaseLayout>
